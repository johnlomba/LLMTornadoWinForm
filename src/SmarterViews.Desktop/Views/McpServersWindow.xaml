<Window x:Class="SmarterViews.Desktop.Views.McpServersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:SmarterViews.Desktop.ViewModels"
        xmlns:models="clr-namespace:SmarterViews.Desktop.Models.Chat"
        xmlns:converters="clr-namespace:SmarterViews.Desktop.Converters"
        mc:Ignorable="d"
        Title="MCP Server Settings" 
        Height="650" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        ResizeMode="CanResize"
        MinWidth="700"
        MinHeight="500"
        d:DataContext="{d:DesignInstance Type=vm:McpServersViewModel}">
    
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        <converters:ArrayToStringConverter x:Key="ArrayToStringConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        
        <!-- Status color style for ellipse -->
        <Style x:Key="StatusEllipseStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="10" />
            <Setter Property="Height" Value="10" />
            <Setter Property="Fill" Value="#6C7086" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Connected">
                    <Setter Property="Fill" Value="#A6E3A1" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                    <Setter Property="Fill" Value="#F9E2AF" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Error">
                    <Setter Property="Fill" Value="#F38BA8" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Button template style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox style -->
        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="CaretBrush" Value="#89B4FA"/>
        </Style>
        
        <!-- ComboBox style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="MCP Server Settings"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#CDD6F4" />
                <TextBlock Text="Configure Model Context Protocol servers for extended tool capabilities"
                           Foreground="#6C7086"
                           FontSize="12"
                           Margin="0,4,0,0"/>
            </StackPanel>
            
            <Button Grid.Column="1"
                    Command="{Binding InitializeAllServersCommand}"
                    Content="Initialize All Servers"
                    Padding="16,10"
                    Background="#89B4FA"
                    Foreground="#1E1E2E"
                    FontWeight="SemiBold"
                    Style="{StaticResource RoundedButtonStyle}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
        </Grid>
        
        <!-- Main content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Server list panel -->
            <Border Grid.Column="0" 
                    Background="#181825"
                    CornerRadius="8"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <Button Grid.Row="0"
                            Command="{Binding AddServerCommand}"
                            Content="+ Add Server"
                            Padding="12,10"
                            Margin="0,0,0,12"
                            Background="#89B4FA"
                            Foreground="#1E1E2E"
                            FontWeight="SemiBold"
                            Style="{StaticResource RoundedButtonStyle}"
                            HorizontalAlignment="Stretch"/>
                    
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Servers}"
                             SelectedItem="{Binding SelectedServer}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Border" 
                                                    Background="#252536"
                                                    CornerRadius="6"
                                                    Padding="12,10"
                                                    Cursor="Hand">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#313244"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#89B4FA"/>
                                                    <Setter TargetName="Border" Property="BorderThickness" Value="1"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#313244"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:McpServerViewModel}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <Ellipse Grid.Column="0"
                                             Style="{StaticResource StatusEllipseStyle}"
                                             Margin="0,0,10,0"
                                             VerticalAlignment="Center" />
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Label}"
                                                   Foreground="#CDD6F4"
                                                   FontWeight="SemiBold"
                                                   FontSize="13" />
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="{Binding Status}"
                                                       Foreground="#6C7086"
                                                       FontSize="11" />
                                            <TextBlock Text=" • "
                                                       Foreground="#6C7086"
                                                       FontSize="11"
                                                       Visibility="{Binding ToolCount, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding ToolCount, StringFormat='{}{0} tools'}"
                                                       Foreground="#6C7086"
                                                       FontSize="11"
                                                       Visibility="{Binding ToolCount, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <Button Grid.Column="2"
                                            Command="{Binding DataContext.RemoveServerCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            Background="Transparent"
                                            Padding="6"
                                            VerticalAlignment="Center"
                                            ToolTip="Remove Server"
                                            Style="{StaticResource RoundedButtonStyle}">
                                        <TextBlock Text="✕" 
                                                   FontSize="12" 
                                                   Foreground="#F38BA8" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            
            <!-- Server details/edit panel -->
            <Border Grid.Column="2" 
                    Background="#181825"
                    CornerRadius="8"
                    Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Error message -->
                        <Border Background="#F38BA8"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,16"
                                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock Text="{Binding ErrorMessage}"
                                       Foreground="#1E1E2E"
                                       TextWrapping="Wrap"
                                       FontWeight="Medium" />
                        </Border>
                        
                        <!-- Server configuration form (visible when adding new or server selected) -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAddingNew}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SelectedServer, Converter={StaticResource NullToVisibilityConverter}}" Value="Visible">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            
                            <TextBlock Text="Server Configuration"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="#CDD6F4"
                                       Margin="0,0,0,16" />
                            
                            <!-- Server Label -->
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Server Label"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <TextBox Text="{Binding NewServerLabel, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource DarkTextBoxStyle}">
                                    <TextBox.IsReadOnly>
                                        <Binding Path="IsAddingNew">
                                            <Binding.Converter>
                                                <converters:InverseBoolConverter/>
                                            </Binding.Converter>
                                        </Binding>
                                    </TextBox.IsReadOnly>
                                </TextBox>
                                <TextBlock Text="A unique name for this server (e.g., 'filesystem', 'weather-api')"
                                           Foreground="#6C7086"
                                           FontSize="11"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <!-- Transport Type -->
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Transport Type"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <ComboBox SelectedValue="{Binding EditingServer.Type, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedValuePath="Content"
                                          Style="{StaticResource DarkComboBoxStyle}">
                                    <ComboBoxItem Content="stdio" />
                                    <ComboBoxItem Content="http" />
                                </ComboBox>
                            </StackPanel>
                            
                            <!-- Stdio configuration -->
                            <StackPanel Margin="0,0,0,16">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EditingServer.Type}" Value="stdio">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                
                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Command"
                                               Foreground="#A6ADC8"
                                               FontSize="12"
                                               Margin="0,0,0,6" />
                                    <TextBox Text="{Binding EditingServer.Command, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource DarkTextBoxStyle}" />
                                    <TextBlock Text="The executable to run (e.g., 'npx', 'node', 'python', 'uvx')"
                                               Foreground="#6C7086"
                                               FontSize="11"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Arguments (one per line)"
                                               Foreground="#A6ADC8"
                                               FontSize="12"
                                               Margin="0,0,0,6" />
                                    <TextBox x:Name="ArgsTextBox"
                                             Text="{Binding EditingServer.Args, Converter={StaticResource ArrayToStringConverter}}"
                                             TextChanged="ArgsTextBox_TextChanged"
                                             Style="{StaticResource DarkTextBoxStyle}"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             MinHeight="80"
                                             VerticalScrollBarVisibility="Auto"/>
                                    <TextBlock Text="Example: -y&#x0a;@anthropic/mcp-server-filesystem"
                                               Foreground="#6C7086"
                                               FontSize="11"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Working Directory (optional)"
                                               Foreground="#A6ADC8"
                                               FontSize="12"
                                               Margin="0,0,0,6" />
                                    <TextBox Text="{Binding EditingServer.WorkingDirectory, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource DarkTextBoxStyle}" />
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- HTTP configuration -->
                            <StackPanel Margin="0,0,0,16">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EditingServer.Type}" Value="http">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                
                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Server URL"
                                               Foreground="#A6ADC8"
                                               FontSize="12"
                                               Margin="0,0,0,6" />
                                    <TextBox Text="{Binding EditingServer.Url, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource DarkTextBoxStyle}" />
                                    <TextBlock Text="The HTTP/SSE endpoint URL (e.g., 'http://localhost:8080')"
                                               Foreground="#6C7086"
                                               FontSize="11"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Allowed Tools -->
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Allowed Tools (optional, comma-separated)"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <TextBox x:Name="AllowedToolsTextBox"
                                         Text="{Binding EditingServer.AllowedTools, Converter={StaticResource ArrayToStringConverter}}"
                                         TextChanged="AllowedToolsTextBox_TextChanged"
                                         Style="{StaticResource DarkTextBoxStyle}"
                                         MinHeight="40" />
                                <TextBlock Text="Leave empty to allow all tools from this server"
                                           Foreground="#6C7086"
                                           FontSize="11"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <!-- Tools list for connected server -->
                            <StackPanel Margin="0,0,0,16"
                                        Visibility="{Binding SelectedServer.ToolCount, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding SelectedServer.ToolCount, StringFormat='Available Tools ({0})'}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="#CDD6F4"
                                           Margin="0,0,0,8" />
                                <Border Background="#252536"
                                        CornerRadius="6"
                                        Padding="12"
                                        MaxHeight="180">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding SelectedServer.Tools}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type models:McpToolViewModel}">
                                                    <Border Background="#313244"
                                                            CornerRadius="4"
                                                            Padding="10,8"
                                                            Margin="0,0,0,6">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Name}"
                                                                       Foreground="#89B4FA"
                                                                       FontWeight="SemiBold"
                                                                       FontSize="12" />
                                                            <TextBlock Text="{Binding Description}"
                                                                       Foreground="#A6ADC8"
                                                                       FontSize="11"
                                                                       TextWrapping="Wrap"
                                                                       Margin="0,4,0,0" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>
                            </StackPanel>
                            
                            <!-- Action buttons -->
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        Margin="0,16,0,0">
                                <Button Command="{Binding TestConnectionCommand}"
                                        Content="Test Connection"
                                        Padding="16,10"
                                        Margin="0,0,12,0"
                                        Background="#F9E2AF"
                                        Foreground="#1E1E2E"
                                        FontWeight="SemiBold"
                                        Style="{StaticResource RoundedButtonStyle}"
                                        Visibility="{Binding SelectedServer, Converter={StaticResource NullToVisibilityConverter}}"/>
                                
                                <Button Command="{Binding SaveServerCommand}"
                                        Content="Save Server"
                                        Padding="16,10"
                                        Margin="0,0,12,0"
                                        Background="#89B4FA"
                                        Foreground="#1E1E2E"
                                        FontWeight="SemiBold"
                                        Style="{StaticResource RoundedButtonStyle}"/>
                                
                                <Button Command="{Binding CancelEditCommand}"
                                        Content="Cancel"
                                        Padding="16,10"
                                        Background="#45475A"
                                        Foreground="#CDD6F4"
                                        Style="{StaticResource RoundedButtonStyle}"
                                        Visibility="{Binding IsAddingNew, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <!-- Empty state -->
                        <StackPanel HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="0,60,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding SelectedServer}" Value="{x:Null}" />
                                                <Condition Binding="{Binding IsAddingNew}" Value="False" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="🔧" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                            <TextBlock Text="Select a server to view details"
                                       Foreground="#6C7086"
                                       FontSize="14"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="or click 'Add Server' to configure a new one"
                                       Foreground="#6C7086"
                                       FontSize="14"
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
        
        <!-- Footer -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Close"
                    Click="CloseButton_Click"
                    Padding="24,12"
                    Background="#45475A"
                    Foreground="#CDD6F4"
                    FontWeight="SemiBold"
                    Style="{StaticResource RoundedButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
