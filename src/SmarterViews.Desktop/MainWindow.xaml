<Window x:Class="SmarterViews.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmarterViews.Desktop"
        xmlns:views="clr-namespace:SmarterViews.Desktop.Views"
        xmlns:vm="clr-namespace:SmarterViews.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="SmarterViews" Height="750" Width="1400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Navigation Tab Style -->
        <Style x:Key="NavTabStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#B3E5FC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="White" BorderThickness="0,0,0,1" BorderBrush="#CCCCCC">
            <MenuItem Header="Tools">
                <MenuItem Header="Database Connections" Click="DatabaseConnections_Click"/>
            </MenuItem>
        </Menu>

        <!-- Header with Navigation Tabs -->
        <Border Grid.Row="1" Background="#1976D2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- App Title -->
                <TextBlock Grid.Column="0" Text="SmarterViews" Foreground="White" FontSize="20" 
                           FontWeight="Bold" VerticalAlignment="Center" Margin="15,10"/>
                
                <!-- Navigation Tabs -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,0,0,0">
                    <RadioButton x:Name="AiQueryTab" Content="🤖 AI Query" 
                                 IsChecked="True" GroupName="NavTabs"
                                 Style="{StaticResource NavTabStyle}"/>
                    <RadioButton x:Name="DatabaseExplorerTab" Content="Database Explorer" 
                                 GroupName="NavTabs"
                                 Style="{StaticResource NavTabStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area with View Switching -->
        <Grid Grid.Row="2">
            <!-- AI Query View (replaces Query Builder) -->
            <views:AiQueryView Visibility="{Binding IsChecked, ElementName=AiQueryTab, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            
            <!-- Database Explorer View -->
            <views:DatabaseExplorerView Visibility="{Binding IsChecked, ElementName=DatabaseExplorerTab, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</Window>
