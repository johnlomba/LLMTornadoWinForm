<UserControl x:Class="LlmTornado.WpfViews.Views.ConversationList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:LlmTornado.WpfViews.ViewModels"
             xmlns:services="clr-namespace:LlmTornado.WpfViews.Services"
             xmlns:converters="clr-namespace:LlmTornado.WpfViews.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="280"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    
    <UserControl.Resources>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Background="#181825">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- New chat button -->
        <Button Grid.Row="0"
                Command="{Binding NewConversationCommand}"
                Margin="12"
                Padding="12,10"
                HorizontalAlignment="Stretch"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" 
                            Background="#252536" 
                            CornerRadius="8" 
                            Padding="{TemplateBinding Padding}"
                            BorderBrush="#45475A"
                            BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#313244" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="#89B4FA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="+" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#89B4FA" 
                           Margin="0,0,8,0" />
                <TextBlock Text="New chat" 
                           Foreground="#CDD6F4" 
                           FontWeight="Medium" />
            </StackPanel>
        </Button>
        
        <!-- Conversations list -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Conversations}"
                 SelectedItem="{Binding SelectedConversation}"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="8,0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="ConversationList_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Border" 
                                        Background="Transparent" 
                                        CornerRadius="8">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#252536" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#313244" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type services:ConversationSummary}">
                    <Grid Margin="12,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- Title -->
                        <TextBlock Grid.Row="0" 
                                   Grid.Column="0"
                                   Text="{Binding Title}"
                                   Foreground="#CDD6F4"
                                   FontSize="13"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="180" />
                        
                        <!-- Delete button -->
                        <Button Grid.Row="0" 
                                Grid.Column="1"
                                Grid.RowSpan="2"
                                Command="{Binding DataContext.DeleteConversationCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                VerticalAlignment="Center"
                                Padding="6,4"
                                Background="Transparent"
                                Cursor="Hand"
                                ToolTip="Delete conversation"
                                Opacity="0.6">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Hidden" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border" 
                                            Background="{TemplateBinding Background}" 
                                            CornerRadius="4" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#F38BA8" />
                                            <Setter Property="Opacity" Value="1" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <TextBlock Text="&#x2715;" 
                                       FontSize="12" 
                                       Foreground="#A6ADC8" />
                        </Button>
                        
                        <!-- Metadata -->
                        <StackPanel Grid.Row="1" 
                                    Grid.Column="0"
                                    Orientation="Horizontal"
                                    Margin="0,4,0,0">
                            <TextBlock Text="{Binding MessageCount, StringFormat='{}{0} messages'}"
                                       Foreground="#6C7086"
                                       FontSize="11" />
                            <TextBlock Text=" · "
                                       Foreground="#6C7086"
                                       FontSize="11" />
                            <TextBlock Text="{Binding UpdatedAt, StringFormat='{}{0:MMM d}'}"
                                       Foreground="#6C7086"
                                       FontSize="11" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <!-- Empty state -->
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="{Binding Conversations.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert, FallbackValue=Collapsed}">
            <TextBlock Text="No conversations yet"
                       Foreground="#6C7086"
                       FontSize="14"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Start a new chat to begin"
                       Foreground="#45475A"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</UserControl>

