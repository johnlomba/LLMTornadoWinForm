<UserControl x:Class="LlmTornado.WpfViews.Views.PromptTemplateDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:LlmTornado.WpfViews.ViewModels"
             xmlns:converters="clr-namespace:LlmTornado.WpfViews.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=vm:PromptTemplateViewModel}">
   
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        <converters:NullToBoolConverter x:Key="NullToBoolConverter" />
        
        <!-- Custom TextBox style for the editor -->
        <Style x:Key="EditorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#252536" />
            <Setter Property="Foreground" Value="#CDD6F4" />
            <Setter Property="CaretBrush" Value="#CDD6F4" />
            <Setter Property="BorderBrush" Value="#45475A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="SelectionBrush" Value="#89B4FA" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#89B4FA" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
   
    <Border Background="#1E1E2E" 
            CornerRadius="12" 
            BorderBrush="#45475A"
            BorderThickness="1"
            Padding="24"
            MinWidth="600"
            MaxWidth="800"
            MinHeight="500"
            MaxHeight="700">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" 
                              ShadowDepth="0" 
                              Opacity="0.5" 
                              Color="Black" />
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="System Prompt Templates"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#CDD6F4" />
                
                <Button Grid.Column="1"
                        Click="CloseButton_Click"
                        Background="Transparent"
                        Padding="8"
                        Cursor="Hand"
                        ToolTip="Close">
                    <TextBlock Text="&#x2715;" 
                               FontSize="16" 
                               Foreground="#A6ADC8" />
                </Button>
            </Grid>
            
            <!-- Content -->
            <Grid Grid.Row="1" MinHeight="350">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Template list -->
                <Border Grid.Column="0" 
                        Background="#181825" 
                        CornerRadius="8"
                        Margin="0,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        
                        <!-- New template button -->
                        <Button Grid.Row="0"
                                Command="{Binding NewTemplateCommand}"
                                Margin="8"
                                Padding="10,8"
                                HorizontalAlignment="Stretch"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border" 
                                            Background="#252536" 
                                            CornerRadius="6" 
                                            Padding="{TemplateBinding Padding}"
                                            BorderBrush="#45475A"
                                            BorderThickness="1">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#313244" />
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#89B4FA" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="+" 
                                           FontSize="14" 
                                           FontWeight="Bold" 
                                           Foreground="#89B4FA" 
                                           Margin="0,0,6,0" />
                                <TextBlock Text="New Template" 
                                           Foreground="#CDD6F4" 
                                           FontSize="12" />
                            </StackPanel>
                        </Button>
                        
                        <!-- Templates list -->
                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding Templates}"
                                 SelectedItem="{Binding SelectedTemplate}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="4"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,1" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Border" 
                                                        Background="Transparent" 
                                                        CornerRadius="6"
                                                        Padding="10,8">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#252536" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#313244" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="#CDD6F4"
                                                   FontSize="12"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
                
                <!-- Template editor -->
                <Border Grid.Column="1" 
                        Background="#181825" 
                        CornerRadius="8"
                        Padding="16">
                    <Grid>
                        <!-- Empty state when no template selected -->
                        <TextBlock Text="Select a template from the list or create a new one"
                                   Foreground="#6C7086"
                                   FontSize="13"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Visibility="{Binding SelectedTemplate, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}" />
                        
                        <!-- Editor when template is selected -->
                        <Grid Visibility="{Binding SelectedTemplate, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <!-- Built-in indicator -->
                            <Border Grid.Row="0"
                                    Background="#45475A"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    HorizontalAlignment="Left"
                                    Margin="0,0,0,12"
                                    Visibility="{Binding SelectedTemplate.IsBuiltIn, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Built-in (read-only)"
                                           Foreground="#A6ADC8"
                                           FontSize="11" />
                            </Border>
                            
                            <!-- Name -->
                            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                                <TextBlock Text="Name"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding SelectedTemplate.IsBuiltIn, Converter={StaticResource InverseBoolConverter}}"
                                         Style="{StaticResource EditorTextBoxStyle}" />
                            </StackPanel>
                            
                            <!-- Description -->
                            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                                <TextBlock Text="Description (optional)"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding SelectedTemplate.IsBuiltIn, Converter={StaticResource InverseBoolConverter}}"
                                         Style="{StaticResource EditorTextBoxStyle}" />
                            </StackPanel>
                            
                            <!-- Content -->
                            <Grid Grid.Row="3" Margin="0,0,0,12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0"
                                           Text="System Prompt Content"
                                           Foreground="#A6ADC8"
                                           FontSize="12"
                                           Margin="0,0,0,6" />
                                <TextBox Grid.Row="1"
                                         Text="{Binding EditContent, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding SelectedTemplate.IsBuiltIn, Converter={StaticResource InverseBoolConverter}}"
                                         Style="{StaticResource EditorTextBoxStyle}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         VerticalScrollBarVisibility="Auto"
                                         VerticalContentAlignment="Top"
                                         MinHeight="120" />
                            </Grid>
                            
                            <!-- Action buttons -->
                            <StackPanel Grid.Row="4" 
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        Visibility="{Binding SelectedTemplate.IsBuiltIn, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                                <Button Content="Delete"
                                        Command="{Binding DeleteTemplateCommand}"
                                        Margin="0,0,8,0"
                                        Padding="14,8"
                                        Background="#F38BA8"
                                        Foreground="#1E1E2E"
                                        Cursor="Hand"
                                        Visibility="{Binding IsNewTemplate, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Border" 
                                                    Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Opacity" Value="0.85" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                
                                <Button Content="Save"
                                        Command="{Binding SaveTemplateCommand}"
                                        Padding="14,8"
                                        Background="#89B4FA"
                                        Foreground="#1E1E2E"
                                        FontWeight="SemiBold"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Border" 
                                                    Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#B4BEFE" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Footer -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                <Button Content="Use Selected"
                        Click="UseSelectedButton_Click"
                        Padding="20,10"
                        Background="#89B4FA"
                        Foreground="#1E1E2E"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        IsEnabled="{Binding SelectedTemplate, Converter={StaticResource NullToBoolConverter}}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="8" 
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#B4BEFE" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
