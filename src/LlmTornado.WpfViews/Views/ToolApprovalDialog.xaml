<UserControl x:Class="LlmTornado.WpfViews.Views.ToolApprovalDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LlmTornado.WpfViews.ViewModels"
             xmlns:converters="clr-namespace:LlmTornado.WpfViews.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="550"
             d:DataContext="{d:DesignInstance Type=vm:ToolApprovalViewModel}">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        
        <!-- Custom TextBox style for arguments display -->
        <Style x:Key="ArgumentsTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#11111B" />
            <Setter Property="Foreground" Value="#A6E3A1" />
            <Setter Property="FontFamily" Value="Cascadia Code, Consolas, Courier New" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderBrush" Value="#45475A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Border Background="#1E1E2E" 
            CornerRadius="12" 
            BorderBrush="#F9E2AF"
            BorderThickness="2"
            Padding="24"
            MinWidth="500"
            MaxWidth="650"
            MaxHeight="500">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" 
                              ShadowDepth="0" 
                              Opacity="0.6" 
                              Color="#F9E2AF" />
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Header with warning icon -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <Border Background="#F9E2AF" 
                        CornerRadius="20" 
                        Width="40" Height="40"
                        Margin="0,0,12,0">
                    <TextBlock Text="⚠" 
                               FontSize="20" 
                               Foreground="#1E1E2E"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Tool Execution Request"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="#CDD6F4" />
                    <TextBlock Text="The AI wants to execute the following tool"
                               FontSize="12"
                               Foreground="#6C7086" />
                </StackPanel>
            </StackPanel>
            
            <!-- Tool name -->
            <Border Grid.Row="1" 
                    Background="#313244" 
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               Text="Tool Name:"
                               Foreground="#A6ADC8"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0" />
                    
                    <Border Grid.Column="1" 
                            Background="#89B4FA" 
                            CornerRadius="4"
                            Padding="10,6"
                            HorizontalAlignment="Left">
                        <TextBlock Text="{Binding CurrentRequest.ToolName}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#1E1E2E" />
                    </Border>
                </Grid>
            </Border>
            
            <!-- Arguments section -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0"
                           Text="Arguments:"
                           Foreground="#A6ADC8"
                           FontSize="13"
                           Margin="0,0,0,8" />
                
                <TextBox Grid.Row="1"
                         Text="{Binding CurrentRequest.FormattedArguments, Mode=OneWay}"
                         Style="{StaticResource ArgumentsTextBoxStyle}"
                         MinHeight="120"
                         MaxHeight="200" />
            </Grid>
            
            <!-- Action buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <!-- Info text -->
                <TextBlock Grid.Column="0"
                           Text="Review carefully before approving"
                           Foreground="#6C7086"
                           FontSize="11"
                           VerticalAlignment="Center" />
                
                <!-- Deny button -->
                <Button Grid.Column="1"
                        Content="Deny"
                        Command="{Binding DenyCommand}"
                        Margin="0,0,12,0"
                        Padding="20,12"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" 
                                    Background="#F38BA8" 
                                    CornerRadius="8" 
                                    Padding="{TemplateBinding Padding}">
                                <TextBlock Text="{TemplateBinding Content}"
                                           Foreground="#1E1E2E"
                                           FontWeight="SemiBold"
                                           FontSize="13"
                                           HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#EBA0AC" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <!-- Approve button -->
                <Button Grid.Column="2"
                        Content="Approve"
                        Command="{Binding ApproveCommand}"
                        Padding="20,12"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" 
                                    Background="#A6E3A1" 
                                    CornerRadius="8" 
                                    Padding="{TemplateBinding Padding}">
                                <TextBlock Text="{TemplateBinding Content}"
                                           Foreground="#1E1E2E"
                                           FontWeight="SemiBold"
                                           FontSize="13"
                                           HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#94E2D5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>

